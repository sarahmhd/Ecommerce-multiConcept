"use client";
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#f5f5f5", // Customize primary color
    },
    secondary: {
      main: "#fdd62d", // Customize secondary color
    },
    third: {
      main: "#d30832",
    },
  },
});

export default theme;
